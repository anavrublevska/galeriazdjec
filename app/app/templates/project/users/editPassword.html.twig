{% extends 'base.html.twig' %}

{% block title %}
    {{ 'edit_password'|trans({'%id%': user.id|default('')}) }}
{% endblock %}

{% block body %}
    {% if user is defined %}
    <h1>{{ 'edit_password'|trans({'%id%': user.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('edit_password', {id: user.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-outline-success" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('users_index') }}" class="btn btn-outline-secondary">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
    {% endif %}
{% endblock %}